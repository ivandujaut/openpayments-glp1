[project]
name = "openpayments-glp1"
version = "0.1.0"
description = "Análisis comercial del mercado GLP-1 (Novo vs. Lilly) sobre CMS Open Payments. Regenerable = auditable."
requires-python = ">=3.12"
dependencies = [
  "duckdb>=1.1",
  "pandas>=2.2",
  "pyarrow>=17",
  "matplotlib>=3.9",
  "requests>=2.32",
  "numpy>=2.5.2",
]
